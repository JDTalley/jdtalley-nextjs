'use client';
import styled from 'styled-components';

const UnorderedList = styled.ul`
  font-size: 1.125rem;
  line-height: 1.6;
  //list-style: none;

  li {
    margin-bottom: 4px;
    font-size: 1.25rem;
  }
`;

export default UnorderedList;
