'use client';
import styled from 'styled-components';

const Nav = styled.nav`
  display: flex;
  justify-content: space-between;
  max-width: 1000px;
  flex: 1;
  margin: 0 auto;
`;

export default Nav;
